apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-nginx-realtest
spec:
  destination:
    name: ''
    namespace: realtest
    server: 'https://kubernetes.default.svc'
  source:
    path: .
    repoURL: 'https://github.com/dinkol/nginx-aks-deployment.git'
    targetRevision: realtest
  sources: []
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
